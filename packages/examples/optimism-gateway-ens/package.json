{
    "version": "0.2.0",
    "description": "A protocol for contract-initiated offchain data retrieval",
    "engines": {
        "node": ">=14"
    },
    "subpackages": "contracts server client",
    "scripts": {
        "preinstall": "run(){ for d in $npm_package_subpackages; do cd $d; yarn install $* || exit; cd -; done; }; run",
        "test": "run(){ for d in $npm_package_subpackages; do cd $d; yarn test $* || exit; cd -; done; }; run",
        "lint": "run(){ for d in $npm_package_subpackages; do cd $d; yarn lint $* || exit; cd -; done; }; run",
        "build": "run(){ for d in $npm_package_subpackages; do cd $d; yarn build $* || exit; cd -; done; }; run",
        "clean": "run(){ for d in $npm_package_subpackages; do cd $d; yarn clean $* || exit; cd -; done; }; run"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/smartcontractkit/ccip-read.js.git"
    },
    "author": "@smartcontractkit",
    "license": "MIT",
    "bugs": {
        "url": "https://github.com/smartcontractkit/ccip-read/issues"
    },
    "peerDependencies": {},
    "homepage": "https://github.com/smartcontractkit/ccip-read#readme"
}
